# 13 · 系统设计实战

> 达标目标：**L3——任何系统设计题能按五步法走完 15 分钟，经典题有完整方案。**
> 一句话理由：系统设计题是全栈 + AI 面试的分水岭，考的是"把模糊需求变成可扩展架构"的判断力——AI 时代这道题更值钱（AI 会给方案，你要能判断）。
> 衔接：12 章（缓存/分布式）、09 章（API/幂等）、11 章（数据库）、15/16 章（AI）。

---

## 1. 方法论：五步法（L3 硬门槛）

### 1.1 五步框架

```
① 澄清需求（3 分钟）—— 规模/读写比/一致性/可用性/功能范围
② 估算（2 分钟）—— QPS、存储、带宽
③ 高层设计（5 分钟）—— 画组件图：LB → 服务 → DB/缓存/MQ/CDN
④ 深入细节（3 分钟）—— 数据模型、接口、读路径/写路径
⑤ 瓶颈与扩展（2 分钟）—— 哪里先挂 → 缓存/分片/异步化
```

### 1.2 常见扣分点（必背）

| 扣分行为 | 正确做法 |
|---|---|
| 不问规模直接开答 | 先问"QPS 多少、读多写多、一致性要求"——**需求不同架构天差地别** |
| 不画图纯口述 | 在白板/纸上画组件图，边说边画 |
| 一上来抠细节 | 先大图后细节：高层设计完整后再深入某条路径 |
| 不提瓶颈 | 主动说"这里会先挂，我的对策是……" |
| 只会加缓存 | 缓存/分片/MQ 都要能说出"解决什么、代价是什么" |

## 2. 估算基本功（L2/L3，能算出量级）

### 2.1 常用估算公式

```
QPS = 日请求量 / 86400 × 峰值系数（通常 3-10 倍）
存储 = 条目数 × 单条大小 × 冗余系数（约 1.5-3）
带宽 = QPS × 平均响应体大小
```

### 2.2 完整示例（短网址，背下来）

```
假设：1 亿用户、10% 日活、每人每天生成 1 条短链 + 访问 10 次

写入 QPS：1 亿 × 10% × 1 / 86400 ≈ 116/s × 峰值系数 ≈ 500/s
读取 QPS：1 亿 × 10% × 10 / 86400 ≈ 1157/s × 峰值系数 ≈ 5000/s
读:写 ≈ 10:1  → 缓存是重点
存储（1 年）：写入量 1 亿×10%×1×365 = 3.65 亿条 × 500B ≈ 18GB
→ 结论：单库足够、Redis 扛读、不需要分库
```

- **量级参考**：单机 MySQL 几千 QPS、Redis 十万 QPS、单机 Node 几千 RPS、Nginx 数万并发连接
- 面试点：**量级对即可**（±10 倍可接受），关键是"有依据 + 推导过程"

## 3. 经典题详解（每题 15 分钟标准答案）

### 3.1 短网址系统（最经典，必会）

**① 需求**：生成短链、访问跳转、可过期
**② 估算**：见 2.2，读远大于写 → 缓存重点
**③ 高层设计**：

```
客户端 → LB → URL 服务（生成/跳转）
                ├→ Redis（热点映射缓存）
                └→ DB（long_url ↔ short_code，唯一约束）
```

**④ 细节**：

```sql
-- 短码生成：62 进制（0-9a-zA-Z）自增 id 编码，7 位 ≈ 62^7 ≈ 3.5 万亿
-- 或：随机短码 + 唯一约束冲突重试（简单）
CREATE TABLE short_urls (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  short_code VARCHAR(7) UNIQUE NOT NULL,   -- 唯一约束（幂等，衔接 09 章）
  long_url TEXT NOT NULL,
  expires_at DATETIME NULL,
  created_at DATETIME NOT NULL
);
```

- **读路径**：`GET /:code` → Redis 查 → miss 查 DB → 写回缓存 → 302 跳转
- **写路径**：`POST /api/shorten` → 生成短码 → DB 插入 → 返回

**⑤ 瓶颈与扩展**：
- 读瓶颈 → Redis 缓存（命中率 90%+）+ CDN（静态跳转页）
- 过期清理 → 惰性（访问时判断）+ 定期批量删
- 短码用尽 → 7 位够万亿，加位即可

### 3.2 Feed 流（推拉结合，L3 必会）

| 方案 | 原理 | 问题 |
|---|---|---|
| 推（写扩散） | 发布时写进所有粉丝的时间线 | 大 V 千万粉丝 → 一次写千万条 |
| 拉（读扩散） | 阅读时合并关注列表的 feed | 读放大，热点用户查询重 |
| **推拉结合** | 普通用户推；**大 V 粉丝拉** | 主流方案 |

```
普通用户发布 → 写进粉丝 inbox（推）
大 V 发布 → 只写大 V 的 outbox，粉丝读时拉取合并
缓存：粉丝 inbox 存 Redis List，超 800 条截断（只保留最近）
```

### 3.3 限流器系统（衔接 09 章，分布式版）

```
单机：令牌桶（内存）
分布式：Redis INCR + Lua 脚本（原子，多实例共享计数）
架构：网关层（Nginx/API Gateway）按 IP/用户限流 → 业务层兜底
数据：redis 计数 key（rate:{id}:{window}），滑动窗口用 zset
```

- 面试点：能说"**限流要在网关做一层 + 业务做一层**"（纵深）

### 3.4 AI 问答产品（全栈 + AI 王炸题，务必练熟）

**① 需求**：基于个人知识库的问答（RAG）、流式回答、成本可控
**② 估算**：QPS（低，个人站）→ 重点是**成本与延迟**不是并发
**③ 高层设计**：

```
用户 → Web(SSE 流式) → AI 网关 → 检索服务 → 向量库/全文库
                        └→ LLM API（厂商）→ 流式回传
缓存：问题哈希命中 → 直接回（省 token）
降级：LLM 挂 → 提示稍后 / 简化模型 / 本地模型兜底
```

**④ 细节**（衔接 15/16 章）：
- 检索：关键词（FTS）+ 向量混合 → rerank → top-5 进 prompt
- **成本控制**：prompt 缓存、相同问题缓存、token 预算上限
- **降级路径**：LLM 不可用 → 返回检索结果摘要（不空手）
- **可观测**：每次调用记录 token/耗时/成功（衔接 06 章网关思路）

**⑤ 瓶颈**：LLM 延迟是瓶颈 → 流式 + 首 token 优化；成本是瓶颈 → 缓存 + 预算

### 3.5 知识库搜索（企业级案例，可延伸）

```
MySQL FULLTEXT（或 Elasticsearch）→ 缓存热词结果 → 分页
→ 后续升级：向量检索（RAG 接入点，衔接 15 章）
```

## 4. 前端视角加分（全栈面试差异化）

- 能谈"这套 API 前端怎么消费"：
  - **首屏字段裁剪**：详情接口别返回 50 个字段给列表用
  - **分页/缓存策略**：列表 cursor 分页 + 请求缓存（衔接 05/09 章）
  - **弱网降级**：骨架屏、错误重试、离线缓存（衔接 17 章）
  - **流式交互**：AI 场景 SSE 逐字渲染（衔接 17 章）
- 面试价值：大多数候选人只讲后端，你能补"前端怎么接"就立住了

## 5. 面试高频题速答

| 题 | 速答要点 |
|---|---|
| 系统设计先做什么 | 澄清需求（规模/读写比/一致性/可用性） |
| 估算说什么 | QPS/存储/带宽，量级对即可，有推导 |
| 高层设计画什么 | LB → 服务 → DB/缓存/MQ/CDN |
| 短网址怎么存 | 62 进制/随机短码 + 唯一约束；Redis 缓存读 |
| Feed 推还是拉 | 推拉结合：普通推、大 V 拉 |
| 限流分布式怎么做 | Redis INCR/Lua；网关 + 业务两层 |
| 数据量 ×100 会怎样 | 缓存 → 读写分离 → 分片（最后手段） |
| AI 产品设计重点 | 成本 + 延迟 + 降级 + 可观测 |

## 6. 达标标准

**L2：**
- [ ] 独立完成一个系统设计题（五步法，15 分钟，先问再答）

**L3：**
- [ ] 完整设计"AI 问答产品"（RAG + 缓存 + 成本 + 降级 + 流式）
- [ ] 短网址 / Feed 流各完整走一遍
- [ ] 每个设计都能回答"数据量 ×100 会怎样"
